# Git Notes
git init
git status
git add .
git commit -m "Create initial tram stop prototype"
git config --global user.name "yangkaifa"
git config --global user.email "180100695+yangkaifa@users.noreply.github.com"
git add notes.md
git commit -m "Add Git learning notes"
git remote add origin https://github.com/yangkaifa/tram-stop-space.git
git branch -M main